export interface Cards {
  title: string;
  description: string;
  complete: boolean;
  image?: NodeRequire;
}

export interface CardList {
  title: string;
  description: string;
  complete: boolean;
  image?: string;
}
